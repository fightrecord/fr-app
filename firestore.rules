rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /channel/{channelId} {
      allow read: if true;
    }

    match /channel/{channelId}/chat/{chatId} {
      allow read, write: if true;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}